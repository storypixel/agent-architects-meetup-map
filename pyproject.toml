[project]
name = "agent-architects-meetup-map"
version = "0.1.0"
description = "Scrape + cluster Skool community member coordinates into meetup hubs"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "scikit-learn>=1.5",
    "numpy>=2.0",
    "reverse_geocoder>=1.5",
    "playwright>=1.50",
]

[tool.uv]
package = false
